Installation: npm install
Server: node app.js

The website requires Database, please configure database with mongoDB.
Please configure the database by creating ".env" file by

NODE_ENV = "Development"
PORT = DESIRED PORT
DATABASE = "ADDRESS TO DATABASE"

Checkout the local server at port 3000 if .env is not configured.
